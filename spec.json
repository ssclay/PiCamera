{
  "nio/Picamera": {
    "version": "0.1.0",
    "description": "",
    "categories": [
      "Hardware"
    ],
    "properties": {
      "file_name": {
        "title": "Image Name",
        "type": "StringType",
        "description": "The name to be used for saving each picture.",
        "default": "image"
      },
      "file_type": {
        "title": "File Type",
        "type": "SelectType",
        "description": "The type of file to save the image as.",
        "default": "jpeg"
      },
      "preview": {
        "title": "Open Preview Window",
        "type": "BoolType",
        "description": "If checked, a preview window will open on the Raspberry Pi that displays a real time stream of the camera.",
        "default": false
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals."
      }
    },
    "outputs": {
      "default": {
        "description": "The same list of signals."
      }
    },
    "commands": {}
  }
}